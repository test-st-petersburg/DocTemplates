name: Build and test
on: [push]
jobs:
  build-and-test:
    name: Build and test
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install required PowerShell modules
        uses: Amadevus/pwsh-script@v2
        id: InstallDependencies
        with:
          script: |
            Set-PSRepository -Name PSGallery -InstallationPolicy Trusted;
            [Net.ServicePointManager]::SecurityProtocol =
              [Net.SecurityProtocolType]::Tls12 -bor `
              [Net.SecurityProtocolType]::Tls11 -bor `
              [Net.SecurityProtocolType]::Tls;
            Install-Module PackageManagement -RequiredVersion 1.3.1;
            Install-Module PowerShellModule;
            Install-Module -Name InvokeBuild -Verbose;
            Install-Module -Name 7Zip4Powershell -Verbose;

      - name: Install choco packages
        run: |
          choco install gitversion.portable -y

      - uses: actions/setup-java@v2
        with:
          distribution: "adopt"
          java-version: "8"
          check-latest: true

      - name: Install NuGet
        uses: nuget/setup-nuget@v1
        with:
          nuget-version: "latest"

      - name: Install NuGet packages
        run: nuget restore tools\packages.config -NonInteractive -PackagesDirectory tools\packages -Source https://api.nuget.org/v3/index.json

      - name: Build .ott and .odt files
        uses: Amadevus/pwsh-script@v2
        id: Build
        with:
          script: |
            Import-Module -Name InvokeBuild;
            Invoke-Build -Task Build -Verbose;
